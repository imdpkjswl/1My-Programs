Take two integers indicating the x and y coordinate of two dimensional graph paper where
the center point is x=0 and y=0.Now print the quadrant of the given point.[if user gives 
input (4,5) you should print 'First Quadrant'; if user gives input (-4,-5) you should print 'Third Quadrant']